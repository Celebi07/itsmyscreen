export default function NotFoundPage() {
  return (
    <main>
      <section className="card">
        <h1>Poll not found</h1>
        <p>The room code is invalid or this poll no longer exists.</p>
      </section>
    </main>
  );
}
